# Architecture

The application has three main parts:

- Flask handles routes and backend behavior.
- The HTML template provides the page structure.
- JavaScript handles browser interaction with the API.

This separation keeps frontend and backend responsibilities clear and makes future changes easier to manage.
