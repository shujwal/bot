# API Example

The chat endpoint can be tested with curl:

```bash
curl -X POST http://127.0.0.1:5000/chat -H "Content-Type: application/json" -d '{"message":"Hello"}'
```

The response is returned as JSON.
