# Testing Guide

Useful tests should cover:

- the index route
- successful chat requests
- missing message data
- invalid message data
- response JSON structure

Flask's test client can test routes without starting a public server.
