# Frontend and Backend Responsibilities

The frontend handles presentation and user interaction.

The Flask backend handles routing and API responses.

Keeping this boundary clear allows each part of the project to evolve independently.
