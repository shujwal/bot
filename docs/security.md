# Security Notes

Before production deployment, review request validation, debug mode, secret handling, request size limits, logging, and deployment configuration.

Never commit API keys, passwords, tokens, or other secrets.
