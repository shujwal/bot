# Troubleshooting

## Flask is not installed

Activate the virtual environment and run:

```bash
pip install -r requirements.txt
```

## Port already in use

Stop the process using the port or run the application on another port.

## API request fails

Confirm the Flask server is running and the frontend is using the expected local address.
